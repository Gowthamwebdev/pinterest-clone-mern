{
  "name": "pinterest-clone",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
  "dev": "concurrently \"pnpm --filter client run dev\" \"pnpm --filter server run start:dev\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}